#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>

/**
 * Includes all my prototypes for argc and argv.
 * @c: Character.
 * @argc: the number of arguments of the file.
 * @argv: the array of arguments.
 */

int _putchar(char c);

#endif
