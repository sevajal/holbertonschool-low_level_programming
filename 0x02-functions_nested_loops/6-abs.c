#include "main.h"
/**
 * _abs - print the absolute value of a integer
 * @c: The int to evaluate
 * Return: 0
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (c * -1);
	return (0);
}
